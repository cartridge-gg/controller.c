// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
#include "dojo.hpp"

#include "UniffiJsiTypes.h"
#include <stdexcept>
#include <map>
#include <utility>
#include <iostream>
#include <thread>

namespace react = facebook::react;
namespace jsi = facebook::jsi;

// Calling into Rust.
extern "C" {
    typedef void
    (*UniffiRustFutureContinuationCallback)(
    uint64_t data, 
    int8_t poll_result
    );
    typedef void
    (*UniffiForeignFutureDroppedCallback)(
    uint64_t handle
    );
    typedef void
    (*UniffiCallbackInterfaceFree)(
    uint64_t handle
    );
    typedef uint64_t
    (*UniffiCallbackInterfaceClone)(
    uint64_t handle
    );typedef struct UniffiForeignFutureDroppedCallbackStruct {
        uint64_t handle;
        UniffiForeignFutureDroppedCallback free;
    } UniffiForeignFutureDroppedCallbackStruct;typedef struct UniffiForeignFutureResultU8 {
        uint8_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU8;
    typedef void
    (*UniffiForeignFutureCompleteU8)(
    uint64_t callback_data, 
    UniffiForeignFutureResultU8 result
    );typedef struct UniffiForeignFutureResultI8 {
        int8_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI8;
    typedef void
    (*UniffiForeignFutureCompleteI8)(
    uint64_t callback_data, 
    UniffiForeignFutureResultI8 result
    );typedef struct UniffiForeignFutureResultU16 {
        uint16_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU16;
    typedef void
    (*UniffiForeignFutureCompleteU16)(
    uint64_t callback_data, 
    UniffiForeignFutureResultU16 result
    );typedef struct UniffiForeignFutureResultI16 {
        int16_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI16;
    typedef void
    (*UniffiForeignFutureCompleteI16)(
    uint64_t callback_data, 
    UniffiForeignFutureResultI16 result
    );typedef struct UniffiForeignFutureResultU32 {
        uint32_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU32;
    typedef void
    (*UniffiForeignFutureCompleteU32)(
    uint64_t callback_data, 
    UniffiForeignFutureResultU32 result
    );typedef struct UniffiForeignFutureResultI32 {
        int32_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI32;
    typedef void
    (*UniffiForeignFutureCompleteI32)(
    uint64_t callback_data, 
    UniffiForeignFutureResultI32 result
    );typedef struct UniffiForeignFutureResultU64 {
        uint64_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultU64;
    typedef void
    (*UniffiForeignFutureCompleteU64)(
    uint64_t callback_data, 
    UniffiForeignFutureResultU64 result
    );typedef struct UniffiForeignFutureResultI64 {
        int64_t return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultI64;
    typedef void
    (*UniffiForeignFutureCompleteI64)(
    uint64_t callback_data, 
    UniffiForeignFutureResultI64 result
    );typedef struct UniffiForeignFutureResultF32 {
        float return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultF32;
    typedef void
    (*UniffiForeignFutureCompleteF32)(
    uint64_t callback_data, 
    UniffiForeignFutureResultF32 result
    );typedef struct UniffiForeignFutureResultF64 {
        double return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultF64;
    typedef void
    (*UniffiForeignFutureCompleteF64)(
    uint64_t callback_data, 
    UniffiForeignFutureResultF64 result
    );typedef struct UniffiForeignFutureResultRustBuffer {
        RustBuffer return_value;
        RustCallStatus call_status;
    } UniffiForeignFutureResultRustBuffer;
    typedef void
    (*UniffiForeignFutureCompleteRustBuffer)(
    uint64_t callback_data, 
    UniffiForeignFutureResultRustBuffer result
    );typedef struct UniffiForeignFutureResultVoid {
        RustCallStatus call_status;
    } UniffiForeignFutureResultVoid;
    typedef void
    (*UniffiForeignFutureCompleteVoid)(
    uint64_t callback_data, 
    UniffiForeignFutureResultVoid result
    );
    typedef void
    (*UniffiCallbackInterfaceEntityUpdateCallbackMethod0)(
    uint64_t uniffi_handle, 
    RustBuffer entity, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceEntityUpdateCallbackMethod1)(
    uint64_t uniffi_handle, 
    RustBuffer error, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceEventUpdateCallbackMethod0)(
    uint64_t uniffi_handle, 
    RustBuffer event, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceEventUpdateCallbackMethod1)(
    uint64_t uniffi_handle, 
    RustBuffer error, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0)(
    uint64_t uniffi_handle, 
    RustBuffer balance, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1)(
    uint64_t uniffi_handle, 
    RustBuffer error, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceTokenUpdateCallbackMethod0)(
    uint64_t uniffi_handle, 
    RustBuffer token, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceTokenUpdateCallbackMethod1)(
    uint64_t uniffi_handle, 
    RustBuffer error, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceTransactionUpdateCallbackMethod0)(
    uint64_t uniffi_handle, 
    RustBuffer transaction, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );
    typedef void
    (*UniffiCallbackInterfaceTransactionUpdateCallbackMethod1)(
    uint64_t uniffi_handle, 
    RustBuffer error, 
    void * uniffi_out_return, RustCallStatus* rust_call_status
    );typedef struct UniffiVTableCallbackInterfaceEntityUpdateCallback {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceEntityUpdateCallbackMethod0 on_update;
        UniffiCallbackInterfaceEntityUpdateCallbackMethod1 on_error;
    } UniffiVTableCallbackInterfaceEntityUpdateCallback;typedef struct UniffiVTableCallbackInterfaceEventUpdateCallback {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceEventUpdateCallbackMethod0 on_update;
        UniffiCallbackInterfaceEventUpdateCallbackMethod1 on_error;
    } UniffiVTableCallbackInterfaceEventUpdateCallback;typedef struct UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 on_update;
        UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 on_error;
    } UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback;typedef struct UniffiVTableCallbackInterfaceTokenUpdateCallback {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceTokenUpdateCallbackMethod0 on_update;
        UniffiCallbackInterfaceTokenUpdateCallbackMethod1 on_error;
    } UniffiVTableCallbackInterfaceTokenUpdateCallback;typedef struct UniffiVTableCallbackInterfaceTransactionUpdateCallback {
        UniffiCallbackInterfaceFree uniffi_free;
        UniffiCallbackInterfaceClone uniffi_clone;
        UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 on_update;
        UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 on_error;
    } UniffiVTableCallbackInterfaceTransactionUpdateCallback;
    /*handle*/ uint64_t uniffi_dojo_uniffi_fn_clone_toriiclient(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void uniffi_dojo_uniffi_fn_free_toriiclient(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    /*handle*/ uint64_t uniffi_dojo_uniffi_fn_constructor_toriiclient_new(
        RustBuffer torii_url, 
        RustCallStatus *uniffi_out_err
    );
    /*handle*/ uint64_t uniffi_dojo_uniffi_fn_constructor_toriiclient_new_with_config(
        RustBuffer torii_url, 
        uint64_t max_message_size, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_achievements(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_activities(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_aggregations(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    void uniffi_dojo_uniffi_fn_method_toriiclient_cancel_subscription(
        /*handle*/ uint64_t ptr, 
        uint64_t subscription_id, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_contracts(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_controllers(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_entities(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_event_messages(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_player_achievements(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_publish_message(
        /*handle*/ uint64_t ptr, 
        RustBuffer message, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_publish_message_batch(
        /*handle*/ uint64_t ptr, 
        RustBuffer messages, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_search(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_sql(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_starknet_events(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    uint64_t uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_entity_updates(
        /*handle*/ uint64_t ptr, 
        RustBuffer clause, 
        RustBuffer world_addresses, 
        uint64_t callback, 
        RustCallStatus *uniffi_out_err
    );
    uint64_t uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_event_updates(
        /*handle*/ uint64_t ptr, 
        RustBuffer keys, 
        uint64_t callback, 
        RustCallStatus *uniffi_out_err
    );
    uint64_t uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_balance_updates(
        /*handle*/ uint64_t ptr, 
        RustBuffer contract_addresses, 
        RustBuffer account_addresses, 
        RustBuffer token_ids, 
        uint64_t callback, 
        RustCallStatus *uniffi_out_err
    );
    uint64_t uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_updates(
        /*handle*/ uint64_t ptr, 
        RustBuffer contract_addresses, 
        RustBuffer token_ids, 
        uint64_t callback, 
        RustCallStatus *uniffi_out_err
    );
    uint64_t uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_transaction_updates(
        /*handle*/ uint64_t ptr, 
        RustBuffer filter, 
        uint64_t callback, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_token_balances(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_token_contracts(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_token_transfers(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_tokens(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_transactions(
        /*handle*/ uint64_t ptr, 
        RustBuffer query, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer uniffi_dojo_uniffi_fn_method_toriiclient_worlds(
        /*handle*/ uint64_t ptr, 
        RustBuffer world_addresses, 
        RustCallStatus *uniffi_out_err
    );
    void uniffi_dojo_uniffi_fn_init_callback_vtable_entityupdatecallback(
        UniffiVTableCallbackInterfaceEntityUpdateCallback * vtable
    );
    void uniffi_dojo_uniffi_fn_init_callback_vtable_eventupdatecallback(
        UniffiVTableCallbackInterfaceEventUpdateCallback * vtable
    );
    void uniffi_dojo_uniffi_fn_init_callback_vtable_tokenbalanceupdatecallback(
        UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback * vtable
    );
    void uniffi_dojo_uniffi_fn_init_callback_vtable_tokenupdatecallback(
        UniffiVTableCallbackInterfaceTokenUpdateCallback * vtable
    );
    void uniffi_dojo_uniffi_fn_init_callback_vtable_transactionupdatecallback(
        UniffiVTableCallbackInterfaceTransactionUpdateCallback * vtable
    );
    RustBuffer ffi_dojo_uniffi_rustbuffer_alloc(
        uint64_t size, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer ffi_dojo_uniffi_rustbuffer_from_bytes(
        ForeignBytes bytes, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rustbuffer_free(
        RustBuffer buf, 
        RustCallStatus *uniffi_out_err
    );
    RustBuffer ffi_dojo_uniffi_rustbuffer_reserve(
        RustBuffer buf, 
        uint64_t additional, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_u8(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_u8(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_u8(
        /*handle*/ uint64_t handle
    );
    uint8_t ffi_dojo_uniffi_rust_future_complete_u8(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_i8(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_i8(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_i8(
        /*handle*/ uint64_t handle
    );
    int8_t ffi_dojo_uniffi_rust_future_complete_i8(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_u16(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_u16(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_u16(
        /*handle*/ uint64_t handle
    );
    uint16_t ffi_dojo_uniffi_rust_future_complete_u16(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_i16(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_i16(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_i16(
        /*handle*/ uint64_t handle
    );
    int16_t ffi_dojo_uniffi_rust_future_complete_i16(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_u32(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_u32(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_u32(
        /*handle*/ uint64_t handle
    );
    uint32_t ffi_dojo_uniffi_rust_future_complete_u32(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_i32(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_i32(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_i32(
        /*handle*/ uint64_t handle
    );
    int32_t ffi_dojo_uniffi_rust_future_complete_i32(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_u64(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_u64(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_u64(
        /*handle*/ uint64_t handle
    );
    uint64_t ffi_dojo_uniffi_rust_future_complete_u64(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_i64(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_i64(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_i64(
        /*handle*/ uint64_t handle
    );
    int64_t ffi_dojo_uniffi_rust_future_complete_i64(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_f32(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_f32(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_f32(
        /*handle*/ uint64_t handle
    );
    float ffi_dojo_uniffi_rust_future_complete_f32(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_f64(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_f64(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_f64(
        /*handle*/ uint64_t handle
    );
    double ffi_dojo_uniffi_rust_future_complete_f64(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_rust_buffer(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_rust_buffer(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_rust_buffer(
        /*handle*/ uint64_t handle
    );
    RustBuffer ffi_dojo_uniffi_rust_future_complete_rust_buffer(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    void ffi_dojo_uniffi_rust_future_poll_void(
        /*handle*/ uint64_t handle, 
        UniffiRustFutureContinuationCallback callback, 
        /*handle*/ uint64_t callback_data
    );
    void ffi_dojo_uniffi_rust_future_cancel_void(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_free_void(
        /*handle*/ uint64_t handle
    );
    void ffi_dojo_uniffi_rust_future_complete_void(
        /*handle*/ uint64_t handle, 
        RustCallStatus *uniffi_out_err
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_achievements(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_activities(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_aggregations(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_cancel_subscription(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_contracts(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_controllers(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_entities(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_event_messages(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_player_achievements(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message_batch(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_search(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_sql(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_starknet_events(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_entity_updates(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_event_updates(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_balance_updates(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_updates(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_transaction_updates(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_token_balances(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_token_contracts(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_token_transfers(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_tokens(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_transactions(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_toriiclient_worlds(
    );
    uint16_t uniffi_dojo_uniffi_checksum_constructor_toriiclient_new(
    );
    uint16_t uniffi_dojo_uniffi_checksum_constructor_toriiclient_new_with_config(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_update(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_error(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_update(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_error(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_update(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_error(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_update(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_error(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_update(
    );
    uint16_t uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_error(
    );
    uint32_t ffi_dojo_uniffi_uniffi_contract_version(
    );
}


namespace uniffi::dojo {
template <typename T> struct Bridging;

using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <typename T> struct Bridging<ReferenceHolder<T>> {
  static jsi::Value jsNew(jsi::Runtime &rt) {
    auto holder = jsi::Object(rt);
    return holder;
  }
  static T fromJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker,
                  const jsi::Value &value) {
    auto obj = value.asObject(rt);
    if (obj.hasProperty(rt, "pointee")) {
      auto pointee = obj.getProperty(rt, "pointee");
      return uniffi::dojo::Bridging<T>::fromJs(rt, callInvoker, pointee);
    }
    throw jsi::JSError(
      rt,
      "Expected ReferenceHolder to have a pointee property. This is likely a bug in uniffi-bindgen-react-native"
    );
  }
};
} // namespace uniffi::dojo
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<RustBuffer> {
  static RustBuffer rustbuffer_alloc(int32_t size) {
      RustCallStatus status = { UNIFFI_CALL_STATUS_OK };
      return ffi_dojo_uniffi_rustbuffer_alloc(
          size,
          &status
      );
  }

  static void rustbuffer_free(RustBuffer buf) {
    RustCallStatus status = { UNIFFI_CALL_STATUS_OK };
    ffi_dojo_uniffi_rustbuffer_free(
        buf,
        &status
    );
  }

  static RustBuffer rustbuffer_from_bytes(ForeignBytes bytes) {
    RustCallStatus status = { UNIFFI_CALL_STATUS_OK };
    return ffi_dojo_uniffi_rustbuffer_from_bytes(
      bytes,
      &status
    );
  }

  static RustBuffer fromJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker>,
                           const jsi::Value &value) {
    try {
      auto buffer = uniffi_jsi::Bridging<jsi::ArrayBuffer>::value_to_arraybuffer(rt, value);
      auto bytes = ForeignBytes{
          .len = static_cast<int32_t>(buffer.length(rt)),
          .data = buffer.data(rt),
      };

      // This buffer is constructed from foreign bytes. Rust scaffolding copies
      // the bytes, to make the RustBuffer.
      auto buf = rustbuffer_from_bytes(bytes);
      // Once it leaves this function, the buffer is immediately passed back
      // into Rust, where it's used to deserialize into the Rust versions of the
      // arguments. At that point, the copy is destroyed.
      return buf;
    } catch (const std::logic_error &e) {
      throw jsi::JSError(rt, e.what());
    }
  }

  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker>,
                         RustBuffer buf) {
    // We need to make a copy of the bytes from Rust's memory space into
    // Javascripts memory space. We need to do this because the two languages
    // manages memory very differently: a garbage collector needs to track all
    // the memory at runtime, Rust is doing it all closer to compile time.
    uint8_t *bytes = new uint8_t[buf.len];
    std::memcpy(bytes, buf.data, buf.len);

    // Construct an ArrayBuffer with copy of the bytes from the RustBuffer.
    auto payload = std::make_shared<uniffi_jsi::CMutableBuffer>(
        uniffi_jsi::CMutableBuffer((uint8_t *)bytes, buf.len));
    auto arrayBuffer = jsi::ArrayBuffer(rt, payload);

    // Once we have a Javascript version, we no longer need the Rust version, so
    // we can call into Rust to tell it it's okay to free that memory.
    rustbuffer_free(buf);

    // Finally, return the ArrayBuffer.
    return uniffi_jsi::Bridging<jsi::ArrayBuffer>::arraybuffer_to_value(rt, arrayBuffer);;
  }
};

} // namespace uniffi::dojo

namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<RustCallStatus> {
  static jsi::Value jsSuccess(jsi::Runtime &rt) {
    auto statusObject = jsi::Object(rt);
    statusObject.setProperty(rt, "code", jsi::Value(rt, UNIFFI_CALL_STATUS_OK));
    return statusObject;
  }
  static RustCallStatus rustSuccess(jsi::Runtime &rt) {
    return {UNIFFI_CALL_STATUS_OK};
  }
  static void copyIntoJs(jsi::Runtime &rt,
                         std::shared_ptr<CallInvoker> callInvoker,
                         const RustCallStatus status,
                         const jsi::Value &jsStatus) {
    auto statusObject = jsStatus.asObject(rt);
    if (status.error_buf.data != nullptr) {
      auto rbuf = Bridging<RustBuffer>::toJs(rt, callInvoker,
                                                         status.error_buf);
      statusObject.setProperty(rt, "errorBuf", rbuf);
    }
    if (status.code != UNIFFI_CALL_STATUS_OK) {
      auto code =
          uniffi_jsi::Bridging<uint8_t>::toJs(rt, callInvoker, status.code);
      statusObject.setProperty(rt, "code", code);
    }
  }

  static jsi::Value toJs(jsi::Runtime &rt,
                         std::shared_ptr<CallInvoker> callInvoker,
                         const RustCallStatus &status) {
    auto statusObject = jsi::Object(rt);
    if (status.error_buf.data != nullptr) {
      auto rbuf = Bridging<RustBuffer>::toJs(rt, callInvoker, status.error_buf);
      statusObject.setProperty(rt, "errorBuf", rbuf);
    }
    statusObject.setProperty(rt, "code", uniffi_jsi::Bridging<uint8_t>::toJs(rt, callInvoker, status.code));
    return statusObject;
  }

  static RustCallStatus fromJs(jsi::Runtime &rt,
                               std::shared_ptr<CallInvoker> invoker,
                               const jsi::Value &jsStatus) {
    RustCallStatus status;
    auto statusObject = jsStatus.asObject(rt);
    if (statusObject.hasProperty(rt, "errorBuf")) {
      auto rbuf = statusObject.getProperty(rt, "errorBuf");
      status.error_buf =
          Bridging<RustBuffer>::fromJs(rt, invoker, rbuf);
    }
    if (statusObject.hasProperty(rt, "code")) {
      auto code = statusObject.getProperty(rt, "code");
      status.code = uniffi_jsi::Bridging<uint8_t>::fromJs(rt, invoker, code);
    }
    return status;
  }

  static void copyFromJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> invoker,
                         const jsi::Value &jsStatus, RustCallStatus *status) {
    auto statusObject = jsStatus.asObject(rt);
    if (statusObject.hasProperty(rt, "errorBuf")) {
      auto rbuf = statusObject.getProperty(rt, "errorBuf");
      status->error_buf =
          Bridging<RustBuffer>::fromJs(rt, invoker, rbuf);
    }
    if (statusObject.hasProperty(rt, "code")) {
      auto code = statusObject.getProperty(rt, "code");
      status->code = uniffi_jsi::Bridging<uint8_t>::fromJs(rt, invoker, code);
    }
  }
};

} // namespace uniffi::dojo
// In other uniffi bindings, it is assumed that the foreign language holds on
// to the vtable, which the Rust just gets a pointer to.
// Here, we need to hold on to them, but also be able to clear them at just the
// right time so we can support hot-reloading.
namespace uniffi::dojo::registry {
    template <typename T>
    class VTableHolder {
    public:
        T vtable;
        VTableHolder(T v) : vtable(v) {}
    };

    // Mutex to bind the storage and setting of vtable together.
    // We declare it here, but the lock is taken by callers of the putTable
    // method who are also sending a pointer to Rust.
    static std::mutex vtableMutex;

    // Registry to hold all vtables so they persist even when JS objects are GC'd.
    // The only reason this exists is to prevent a dangling pointer in the
    // Rust machinery: i.e. we don't need to access or write to this registry
    // after startup.
    // Registry to hold all vtables so they persist even when JS objects are GC'd.
    // Maps string identifiers to vtable holders using type erasure
    static std::unordered_map<std::string, std::shared_ptr<void>> vtableRegistry;

    // Add a vtable to the registry with an identifier
    template <typename T>
    static T* putTable(std::string_view identifier, T vtable) {
        auto holder = std::make_shared<VTableHolder<T>>(vtable);
        // Store the raw pointer to the vtable before type erasure
        T* rawPtr = &(holder->vtable);
        // Store the holder using type erasure with the string identifier
        vtableRegistry[std::string(identifier)] = std::shared_ptr<void>(holder);
        return rawPtr;
    }

    // Clear the registry.
    //
    // Conceptually, this is called after teardown of the module (i.e. after
    // teardown of the jsi::Runtime). However, because Rust is dropping callbacks
    // because the Runtime is being torn down, we must keep the registry intact
    // until after the runtime goes away.
    //
    // Therefore, in practice we should call this when the next runtime is
    // being stood up.
    static void clearRegistry() {
        std::lock_guard<std::mutex> lock(vtableMutex);
        vtableRegistry.clear();
    }
} // namespace uniffi::dojo::registry

// This calls into Rust.

// Generate Bridging for ForeignFutureResult structs (needed if module doesn't have async fns)
// Generate Bridging templates for internal UniffiForeignFutureResult structs
// These are used by async callbacks but not exposed in ffi_definitions()
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

#ifndef BRIDGING_UniffiForeignFutureResultU8_DEFINED
#define BRIDGING_UniffiForeignFutureResultU8_DEFINED
template <> struct Bridging<UniffiForeignFutureResultU8> {
  static UniffiForeignFutureResultU8 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU8");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultU8 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU8 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultU8_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultI8_DEFINED
#define BRIDGING_UniffiForeignFutureResultI8_DEFINED
template <> struct Bridging<UniffiForeignFutureResultI8> {
  static UniffiForeignFutureResultI8 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI8");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultI8 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI8 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultI8_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultU16_DEFINED
#define BRIDGING_UniffiForeignFutureResultU16_DEFINED
template <> struct Bridging<UniffiForeignFutureResultU16> {
  static UniffiForeignFutureResultU16 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU16");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultU16 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU16 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultU16_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultI16_DEFINED
#define BRIDGING_UniffiForeignFutureResultI16_DEFINED
template <> struct Bridging<UniffiForeignFutureResultI16> {
  static UniffiForeignFutureResultI16 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI16");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultI16 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI16 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultI16_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultU32_DEFINED
#define BRIDGING_UniffiForeignFutureResultU32_DEFINED
template <> struct Bridging<UniffiForeignFutureResultU32> {
  static UniffiForeignFutureResultU32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU32");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultU32 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU32 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultU32_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultI32_DEFINED
#define BRIDGING_UniffiForeignFutureResultI32_DEFINED
template <> struct Bridging<UniffiForeignFutureResultI32> {
  static UniffiForeignFutureResultI32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI32");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultI32 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI32 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultI32_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultU64_DEFINED
#define BRIDGING_UniffiForeignFutureResultU64_DEFINED
template <> struct Bridging<UniffiForeignFutureResultU64> {
  static UniffiForeignFutureResultU64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU64");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultU64 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU64 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultU64_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultI64_DEFINED
#define BRIDGING_UniffiForeignFutureResultI64_DEFINED
template <> struct Bridging<UniffiForeignFutureResultI64> {
  static UniffiForeignFutureResultI64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI64");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultI64 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI64 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultI64_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultF32_DEFINED
#define BRIDGING_UniffiForeignFutureResultF32_DEFINED
template <> struct Bridging<UniffiForeignFutureResultF32> {
  static UniffiForeignFutureResultF32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultF32");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultF32 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultF32 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultF32_DEFINED

#ifndef BRIDGING_UniffiForeignFutureResultF64_DEFINED
#define BRIDGING_UniffiForeignFutureResultF64_DEFINED
template <> struct Bridging<UniffiForeignFutureResultF64> {
  static UniffiForeignFutureResultF64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultF64");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultF64 rsObject;
    
    // Convert return_value field
    rsObject.return_value = uniffi_jsi::Bridging<decltype(rsObject.return_value)>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultF64 &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      uniffi_jsi::Bridging<decltype(rsValue.return_value)>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultF64_DEFINED

// Special case for Void - no return_value field
#ifndef BRIDGING_UniffiForeignFutureResultVoid_DEFINED
#define BRIDGING_UniffiForeignFutureResultVoid_DEFINED
template <> struct Bridging<UniffiForeignFutureResultVoid> {
  static UniffiForeignFutureResultVoid fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultVoid");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultVoid rsObject;
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultVoid &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultVoid_DEFINED

// Special case for RustBuffer - uses module namespace Bridging, not uniffi_jsi
#ifndef BRIDGING_UniffiForeignFutureResultRustBuffer_DEFINED
#define BRIDGING_UniffiForeignFutureResultRustBuffer_DEFINED
template <> struct Bridging<UniffiForeignFutureResultRustBuffer> {
  static UniffiForeignFutureResultRustBuffer fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultRustBuffer");
    }
    auto jsObject = jsValue.getObject(rt);
    UniffiForeignFutureResultRustBuffer rsObject;
    
    // Convert return_value field - RustBuffer uses module namespace Bridging
    rsObject.return_value = Bridging<RustBuffer>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "return_value")
    );
    
    // Convert call_status field 
    rsObject.call_status = Bridging<RustCallStatus>::fromJs(
        rt, callInvoker, jsObject.getProperty(rt, "call_status")
    );
    
    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultRustBuffer &rsValue
  ) {
    auto jsObject = jsi::Object(rt);
    
    jsObject.setProperty(rt, "return_value",
      Bridging<RustBuffer>::toJs(rt, callInvoker, rsValue.return_value)
    );
    
    jsObject.setProperty(rt, "call_status",
      Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );
    
    return jsObject;
  }
};
#endif // BRIDGING_UniffiForeignFutureResultRustBuffer_DEFINED

} // namespace uniffi::dojo


// FIRST: Declare all callback wrapper structs
#ifndef WRAPPER_DECL_UniffiRustFutureContinuationCallback_DEFINED
#define WRAPPER_DECL_UniffiRustFutureContinuationCallback_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiRustFutureContinuationCallbackWrapper {
    UniffiRustFutureContinuationCallback callback;
    explicit UniffiRustFutureContinuationCallbackWrapper(UniffiRustFutureContinuationCallback cb) : callback(cb) {}
    operator UniffiRustFutureContinuationCallback() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiRustFutureContinuationCallback_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureDroppedCallback_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureDroppedCallback_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureDroppedCallbackWrapper {
    UniffiForeignFutureDroppedCallback callback;
    explicit UniffiForeignFutureDroppedCallbackWrapper(UniffiForeignFutureDroppedCallback cb) : callback(cb) {}
    operator UniffiForeignFutureDroppedCallback() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureDroppedCallback_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceFree_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceFree_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceFreeWrapper {
    UniffiCallbackInterfaceFree callback;
    explicit UniffiCallbackInterfaceFreeWrapper(UniffiCallbackInterfaceFree cb) : callback(cb) {}
    operator UniffiCallbackInterfaceFree() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceFree_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceClone_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceClone_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceCloneWrapper {
    UniffiCallbackInterfaceClone callback;
    explicit UniffiCallbackInterfaceCloneWrapper(UniffiCallbackInterfaceClone cb) : callback(cb) {}
    operator UniffiCallbackInterfaceClone() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceClone_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteU8_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteU8_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteU8Wrapper {
    UniffiForeignFutureCompleteU8 callback;
    explicit UniffiForeignFutureCompleteU8Wrapper(UniffiForeignFutureCompleteU8 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteU8() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteU8_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteI8_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteI8_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteI8Wrapper {
    UniffiForeignFutureCompleteI8 callback;
    explicit UniffiForeignFutureCompleteI8Wrapper(UniffiForeignFutureCompleteI8 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteI8() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteI8_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteU16_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteU16_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteU16Wrapper {
    UniffiForeignFutureCompleteU16 callback;
    explicit UniffiForeignFutureCompleteU16Wrapper(UniffiForeignFutureCompleteU16 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteU16() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteU16_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteI16_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteI16_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteI16Wrapper {
    UniffiForeignFutureCompleteI16 callback;
    explicit UniffiForeignFutureCompleteI16Wrapper(UniffiForeignFutureCompleteI16 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteI16() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteI16_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteU32_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteU32_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteU32Wrapper {
    UniffiForeignFutureCompleteU32 callback;
    explicit UniffiForeignFutureCompleteU32Wrapper(UniffiForeignFutureCompleteU32 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteU32() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteU32_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteI32_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteI32_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteI32Wrapper {
    UniffiForeignFutureCompleteI32 callback;
    explicit UniffiForeignFutureCompleteI32Wrapper(UniffiForeignFutureCompleteI32 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteI32() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteI32_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteU64_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteU64_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteU64Wrapper {
    UniffiForeignFutureCompleteU64 callback;
    explicit UniffiForeignFutureCompleteU64Wrapper(UniffiForeignFutureCompleteU64 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteU64() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteU64_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteI64_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteI64_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteI64Wrapper {
    UniffiForeignFutureCompleteI64 callback;
    explicit UniffiForeignFutureCompleteI64Wrapper(UniffiForeignFutureCompleteI64 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteI64() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteI64_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteF32_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteF32_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteF32Wrapper {
    UniffiForeignFutureCompleteF32 callback;
    explicit UniffiForeignFutureCompleteF32Wrapper(UniffiForeignFutureCompleteF32 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteF32() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteF32_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteF64_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteF64_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteF64Wrapper {
    UniffiForeignFutureCompleteF64 callback;
    explicit UniffiForeignFutureCompleteF64Wrapper(UniffiForeignFutureCompleteF64 cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteF64() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteF64_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteRustBuffer_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteRustBuffer_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteRustBufferWrapper {
    UniffiForeignFutureCompleteRustBuffer callback;
    explicit UniffiForeignFutureCompleteRustBufferWrapper(UniffiForeignFutureCompleteRustBuffer cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteRustBuffer() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteRustBuffer_DEFINED


#ifndef WRAPPER_DECL_UniffiForeignFutureCompleteVoid_DEFINED
#define WRAPPER_DECL_UniffiForeignFutureCompleteVoid_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiForeignFutureCompleteVoidWrapper {
    UniffiForeignFutureCompleteVoid callback;
    explicit UniffiForeignFutureCompleteVoidWrapper(UniffiForeignFutureCompleteVoid cb) : callback(cb) {}
    operator UniffiForeignFutureCompleteVoid() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiForeignFutureCompleteVoid_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceEntityUpdateCallbackMethod0Wrapper {
    UniffiCallbackInterfaceEntityUpdateCallbackMethod0 callback;
    explicit UniffiCallbackInterfaceEntityUpdateCallbackMethod0Wrapper(UniffiCallbackInterfaceEntityUpdateCallbackMethod0 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceEntityUpdateCallbackMethod0() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceEntityUpdateCallbackMethod1Wrapper {
    UniffiCallbackInterfaceEntityUpdateCallbackMethod1 callback;
    explicit UniffiCallbackInterfaceEntityUpdateCallbackMethod1Wrapper(UniffiCallbackInterfaceEntityUpdateCallbackMethod1 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceEntityUpdateCallbackMethod1() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceEventUpdateCallbackMethod0Wrapper {
    UniffiCallbackInterfaceEventUpdateCallbackMethod0 callback;
    explicit UniffiCallbackInterfaceEventUpdateCallbackMethod0Wrapper(UniffiCallbackInterfaceEventUpdateCallbackMethod0 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceEventUpdateCallbackMethod0() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceEventUpdateCallbackMethod1Wrapper {
    UniffiCallbackInterfaceEventUpdateCallbackMethod1 callback;
    explicit UniffiCallbackInterfaceEventUpdateCallbackMethod1Wrapper(UniffiCallbackInterfaceEventUpdateCallbackMethod1 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceEventUpdateCallbackMethod1() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0Wrapper {
    UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 callback;
    explicit UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0Wrapper(UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1Wrapper {
    UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 callback;
    explicit UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1Wrapper(UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceTokenUpdateCallbackMethod0Wrapper {
    UniffiCallbackInterfaceTokenUpdateCallbackMethod0 callback;
    explicit UniffiCallbackInterfaceTokenUpdateCallbackMethod0Wrapper(UniffiCallbackInterfaceTokenUpdateCallbackMethod0 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceTokenUpdateCallbackMethod0() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceTokenUpdateCallbackMethod1Wrapper {
    UniffiCallbackInterfaceTokenUpdateCallbackMethod1 callback;
    explicit UniffiCallbackInterfaceTokenUpdateCallbackMethod1Wrapper(UniffiCallbackInterfaceTokenUpdateCallbackMethod1 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceTokenUpdateCallbackMethod1() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceTransactionUpdateCallbackMethod0Wrapper {
    UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 callback;
    explicit UniffiCallbackInterfaceTransactionUpdateCallbackMethod0Wrapper(UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceTransactionUpdateCallbackMethod0() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED


#ifndef WRAPPER_DECL_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
#define WRAPPER_DECL_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
// Forward declaration of wrapper struct
struct UniffiCallbackInterfaceTransactionUpdateCallbackMethod1Wrapper {
    UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 callback;
    explicit UniffiCallbackInterfaceTransactionUpdateCallbackMethod1Wrapper(UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 cb) : callback(cb) {}
    operator UniffiCallbackInterfaceTransactionUpdateCallbackMethod1() const { return callback; }
};
} // namespace uniffi::dojo
#endif // WRAPPER_DECL_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED



// SECOND: Generate callback implementations (makeCallbackFunction, etc.)
// Structs need these for their fromJs methods
#ifndef CALLBACK_uniffi_dojo_cb_rustfuturecontinuationcallback_UniffiRustFutureContinuationCallback_DEFINED
#define CALLBACK_uniffi_dojo_cb_rustfuturecontinuationcallback_UniffiRustFutureContinuationCallback_DEFINED
// Callback function: uniffi::dojo::cb::rustfuturecontinuationcallback::UniffiRustFutureContinuationCallback
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::rustfuturecontinuationcallback {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, int8_t)> rsLambda_UniffiRustFutureContinuationCallback = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiRustFutureContinuationCallback(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_data
            ,int8_t rs_pollResult) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_data = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_data);
        auto js_pollResult = uniffi_jsi::Bridging<int8_t>::toJs(rt, callInvoker, rs_pollResult);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_data, js_pollResult
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiRustFutureContinuationCallback: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiRustFutureContinuationCallback(uint64_t rs_data, int8_t rs_pollResult) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiRustFutureContinuationCallback.
        //
        // If rsLamda_UniffiRustFutureContinuationCallback is null, then there is no runtime to call into.
        if (rsLambda_UniffiRustFutureContinuationCallback == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiRustFutureContinuationCallback(
            rs_data, 
            rs_pollResult);
    }

    [[maybe_unused]] static UniffiRustFutureContinuationCallback
    makeCallbackFunction( // uniffi::dojo::cb::rustfuturecontinuationcallback
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiRustFutureContinuationCallback != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiRustFutureContinuationCallback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiRustFutureContinuationCallback = [rtPtr, callInvoker, callbackValue](uint64_t rs_data, int8_t rs_pollResult) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_data
                    , rs_pollResult](jsi::Runtime &rt) mutable {
                    body_UniffiRustFutureContinuationCallback(rt, callInvoker, callbackValue
                        , rs_data
                        , rs_pollResult);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiRustFutureContinuationCallback;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiRustFutureContinuationCallback = nullptr;
    }
} // namespace uniffi::dojo::cb::rustfuturecontinuationcallback
#endif // CALLBACK_uniffi_dojo_cb_rustfuturecontinuationcallback_UniffiRustFutureContinuationCallback_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturedroppedcallback_UniffiForeignFutureDroppedCallback_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturedroppedcallback_UniffiForeignFutureDroppedCallback_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturedroppedcallback::UniffiForeignFutureDroppedCallback
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturedroppedcallback {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiForeignFutureDroppedCallback = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureDroppedCallback(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureDroppedCallback: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureDroppedCallback(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureDroppedCallback.
        //
        // If rsLamda_UniffiForeignFutureDroppedCallback is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureDroppedCallback == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureDroppedCallback(
            rs_handle);
    }

    [[maybe_unused]] static UniffiForeignFutureDroppedCallback
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturedroppedcallback
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureDroppedCallback != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureDroppedCallback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureDroppedCallback = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureDroppedCallback(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureDroppedCallback;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureDroppedCallback = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturedroppedcallback
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturedroppedcallback_UniffiForeignFutureDroppedCallback_DEFINED

#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceentityupdatecallback_vtablecallbackinterfaceentityupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceentityupdatecallback_vtablecallbackinterfaceentityupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceentityupdatecallback_vtablecallbackinterfaceentityupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceeventupdatecallback_vtablecallbackinterfaceeventupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceeventupdatecallback_vtablecallbackinterfaceeventupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceeventupdatecallback_vtablecallbackinterfaceeventupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenbalanceupdatecallback_vtablecallbackinterfacetokenbalanceupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenbalanceupdatecallback_vtablecallbackinterfacetokenbalanceupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenbalanceupdatecallback_vtablecallbackinterfacetokenbalanceupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenupdatecallback_vtablecallbackinterfacetokenupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenupdatecallback_vtablecallbackinterfacetokenupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenupdatecallback_vtablecallbackinterfacetokenupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetransactionupdatecallback_vtablecallbackinterfacetransactionupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetransactionupdatecallback_vtablecallbackinterfacetransactionupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetransactionupdatecallback_vtablecallbackinterfacetransactionupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceclone_UniffiCallbackInterfaceClone_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceclone_UniffiCallbackInterfaceClone_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceclone::UniffiCallbackInterfaceClone
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceclone {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<uint64_t(uint64_t)> rsLambda_UniffiCallbackInterfaceClone = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceClone(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceClone: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static uint64_t callback_UniffiCallbackInterfaceClone(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceClone.
        //
        // If rsLamda_UniffiCallbackInterfaceClone is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceClone == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return 0;  // Return zero for handle/uint64_t return types
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        return rsLambda_UniffiCallbackInterfaceClone(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceClone
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceclone
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceClone != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceClone;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceClone = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceClone(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
                return 0;  // Async callback, return immediately
        };
        return callback_UniffiCallbackInterfaceClone;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceClone = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceclone
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceclone_UniffiCallbackInterfaceClone_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu8_UniffiForeignFutureCompleteU8_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu8_UniffiForeignFutureCompleteU8_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompleteu8::UniffiForeignFutureCompleteU8
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompleteu8 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultU8)> rsLambda_UniffiForeignFutureCompleteU8 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteU8(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultU8 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultU8>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteU8: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteU8(uint64_t rs_callbackData, UniffiForeignFutureResultU8 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteU8.
        //
        // If rsLamda_UniffiForeignFutureCompleteU8 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteU8 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteU8(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteU8
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompleteu8
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteU8 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteU8;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteU8 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultU8 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteU8(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteU8;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteU8 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompleteu8
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu8_UniffiForeignFutureCompleteU8_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletei8_UniffiForeignFutureCompleteI8_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletei8_UniffiForeignFutureCompleteI8_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletei8::UniffiForeignFutureCompleteI8
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletei8 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultI8)> rsLambda_UniffiForeignFutureCompleteI8 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteI8(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultI8 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultI8>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteI8: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteI8(uint64_t rs_callbackData, UniffiForeignFutureResultI8 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteI8.
        //
        // If rsLamda_UniffiForeignFutureCompleteI8 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteI8 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteI8(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteI8
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletei8
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteI8 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteI8;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteI8 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultI8 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteI8(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteI8;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteI8 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletei8
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletei8_UniffiForeignFutureCompleteI8_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu16_UniffiForeignFutureCompleteU16_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu16_UniffiForeignFutureCompleteU16_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompleteu16::UniffiForeignFutureCompleteU16
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompleteu16 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultU16)> rsLambda_UniffiForeignFutureCompleteU16 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteU16(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultU16 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultU16>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteU16: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteU16(uint64_t rs_callbackData, UniffiForeignFutureResultU16 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteU16.
        //
        // If rsLamda_UniffiForeignFutureCompleteU16 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteU16 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteU16(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteU16
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompleteu16
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteU16 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteU16;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteU16 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultU16 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteU16(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteU16;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteU16 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompleteu16
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu16_UniffiForeignFutureCompleteU16_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletei16_UniffiForeignFutureCompleteI16_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletei16_UniffiForeignFutureCompleteI16_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletei16::UniffiForeignFutureCompleteI16
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletei16 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultI16)> rsLambda_UniffiForeignFutureCompleteI16 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteI16(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultI16 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultI16>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteI16: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteI16(uint64_t rs_callbackData, UniffiForeignFutureResultI16 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteI16.
        //
        // If rsLamda_UniffiForeignFutureCompleteI16 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteI16 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteI16(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteI16
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletei16
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteI16 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteI16;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteI16 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultI16 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteI16(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteI16;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteI16 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletei16
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletei16_UniffiForeignFutureCompleteI16_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu32_UniffiForeignFutureCompleteU32_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu32_UniffiForeignFutureCompleteU32_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompleteu32::UniffiForeignFutureCompleteU32
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompleteu32 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultU32)> rsLambda_UniffiForeignFutureCompleteU32 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteU32(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultU32 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultU32>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteU32: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteU32(uint64_t rs_callbackData, UniffiForeignFutureResultU32 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteU32.
        //
        // If rsLamda_UniffiForeignFutureCompleteU32 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteU32 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteU32(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteU32
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompleteu32
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteU32 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteU32;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteU32 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultU32 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteU32(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteU32;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteU32 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompleteu32
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu32_UniffiForeignFutureCompleteU32_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletei32_UniffiForeignFutureCompleteI32_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletei32_UniffiForeignFutureCompleteI32_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletei32::UniffiForeignFutureCompleteI32
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletei32 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultI32)> rsLambda_UniffiForeignFutureCompleteI32 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteI32(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultI32 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultI32>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteI32: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteI32(uint64_t rs_callbackData, UniffiForeignFutureResultI32 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteI32.
        //
        // If rsLamda_UniffiForeignFutureCompleteI32 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteI32 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteI32(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteI32
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletei32
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteI32 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteI32;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteI32 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultI32 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteI32(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteI32;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteI32 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletei32
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletei32_UniffiForeignFutureCompleteI32_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu64_UniffiForeignFutureCompleteU64_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu64_UniffiForeignFutureCompleteU64_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompleteu64::UniffiForeignFutureCompleteU64
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompleteu64 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultU64)> rsLambda_UniffiForeignFutureCompleteU64 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteU64(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultU64 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultU64>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteU64: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteU64(uint64_t rs_callbackData, UniffiForeignFutureResultU64 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteU64.
        //
        // If rsLamda_UniffiForeignFutureCompleteU64 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteU64 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteU64(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteU64
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompleteu64
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteU64 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteU64;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteU64 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultU64 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteU64(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteU64;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteU64 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompleteu64
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompleteu64_UniffiForeignFutureCompleteU64_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletei64_UniffiForeignFutureCompleteI64_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletei64_UniffiForeignFutureCompleteI64_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletei64::UniffiForeignFutureCompleteI64
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletei64 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultI64)> rsLambda_UniffiForeignFutureCompleteI64 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteI64(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultI64 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultI64>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteI64: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteI64(uint64_t rs_callbackData, UniffiForeignFutureResultI64 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteI64.
        //
        // If rsLamda_UniffiForeignFutureCompleteI64 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteI64 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteI64(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteI64
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletei64
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteI64 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteI64;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteI64 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultI64 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteI64(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteI64;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteI64 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletei64
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletei64_UniffiForeignFutureCompleteI64_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletef32_UniffiForeignFutureCompleteF32_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletef32_UniffiForeignFutureCompleteF32_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletef32::UniffiForeignFutureCompleteF32
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletef32 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultF32)> rsLambda_UniffiForeignFutureCompleteF32 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteF32(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultF32 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultF32>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteF32: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteF32(uint64_t rs_callbackData, UniffiForeignFutureResultF32 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteF32.
        //
        // If rsLamda_UniffiForeignFutureCompleteF32 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteF32 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteF32(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteF32
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletef32
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteF32 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteF32;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteF32 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultF32 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteF32(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteF32;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteF32 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletef32
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletef32_UniffiForeignFutureCompleteF32_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletef64_UniffiForeignFutureCompleteF64_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletef64_UniffiForeignFutureCompleteF64_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletef64::UniffiForeignFutureCompleteF64
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletef64 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultF64)> rsLambda_UniffiForeignFutureCompleteF64 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteF64(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultF64 rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultF64>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteF64: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteF64(uint64_t rs_callbackData, UniffiForeignFutureResultF64 rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteF64.
        //
        // If rsLamda_UniffiForeignFutureCompleteF64 is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteF64 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteF64(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteF64
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletef64
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteF64 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteF64;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteF64 = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultF64 rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteF64(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteF64;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteF64 = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletef64
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletef64_UniffiForeignFutureCompleteF64_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompleterustbuffer_UniffiForeignFutureCompleteRustBuffer_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompleterustbuffer_UniffiForeignFutureCompleteRustBuffer_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompleterustbuffer::UniffiForeignFutureCompleteRustBuffer
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompleterustbuffer {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultRustBuffer)> rsLambda_UniffiForeignFutureCompleteRustBuffer = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteRustBuffer(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultRustBuffer rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultRustBuffer>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteRustBuffer: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteRustBuffer(uint64_t rs_callbackData, UniffiForeignFutureResultRustBuffer rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteRustBuffer.
        //
        // If rsLamda_UniffiForeignFutureCompleteRustBuffer is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteRustBuffer == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteRustBuffer(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteRustBuffer
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompleterustbuffer
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteRustBuffer != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteRustBuffer;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteRustBuffer = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultRustBuffer rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteRustBuffer(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteRustBuffer;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteRustBuffer = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompleterustbuffer
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompleterustbuffer_UniffiForeignFutureCompleteRustBuffer_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_foreignfuturecompletevoid_UniffiForeignFutureCompleteVoid_DEFINED
#define CALLBACK_uniffi_dojo_cb_foreignfuturecompletevoid_UniffiForeignFutureCompleteVoid_DEFINED
// Callback function: uniffi::dojo::cb::foreignfuturecompletevoid::UniffiForeignFutureCompleteVoid
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::foreignfuturecompletevoid {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, UniffiForeignFutureResultVoid)> rsLambda_UniffiForeignFutureCompleteVoid = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiForeignFutureCompleteVoid(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_callbackData
            ,UniffiForeignFutureResultVoid rs_result) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_callbackData = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_callbackData);
        auto js_result = uniffi::dojo::Bridging<UniffiForeignFutureResultVoid>::toJs(rt, callInvoker, rs_result);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_callbackData, js_result
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiForeignFutureCompleteVoid: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiForeignFutureCompleteVoid(uint64_t rs_callbackData, UniffiForeignFutureResultVoid rs_result) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiForeignFutureCompleteVoid.
        //
        // If rsLamda_UniffiForeignFutureCompleteVoid is null, then there is no runtime to call into.
        if (rsLambda_UniffiForeignFutureCompleteVoid == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiForeignFutureCompleteVoid(
            rs_callbackData, 
            rs_result);
    }

    [[maybe_unused]] static UniffiForeignFutureCompleteVoid
    makeCallbackFunction( // uniffi::dojo::cb::foreignfuturecompletevoid
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiForeignFutureCompleteVoid != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiForeignFutureCompleteVoid;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiForeignFutureCompleteVoid = [rtPtr, callInvoker, callbackValue](uint64_t rs_callbackData, UniffiForeignFutureResultVoid rs_result) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_callbackData
                    , rs_result](jsi::Runtime &rt) mutable {
                    body_UniffiForeignFutureCompleteVoid(rt, callInvoker, callbackValue
                        , rs_callbackData
                        , rs_result);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiForeignFutureCompleteVoid;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiForeignFutureCompleteVoid = nullptr;
    }
} // namespace uniffi::dojo::cb::foreignfuturecompletevoid
#endif // CALLBACK_uniffi_dojo_cb_foreignfuturecompletevoid_UniffiForeignFutureCompleteVoid_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod0_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod0_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0::UniffiCallbackInterfaceEntityUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_entity
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_entity = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_entity);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_entity
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEntityUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_entity, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_entity, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEntityUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_entity, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_entity
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_entity
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod0_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod1_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod1_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1::UniffiCallbackInterfaceEntityUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEntityUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEntityUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod1_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod0_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod0_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0::UniffiCallbackInterfaceEventUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEventUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_event
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_event = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_event);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_event
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEventUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEventUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_event, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_event, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEventUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_event, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_event
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEventUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_event
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod0_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod1_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod1_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1::UniffiCallbackInterfaceEventUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEventUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEventUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEventUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEventUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEventUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod1_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod0_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod0_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0::UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_balance
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_balance = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_balance);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_balance
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_balance, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_balance, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_balance, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_balance
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_balance
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod0_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod1_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod1_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1::UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod1_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod0_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod0_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0::UniffiCallbackInterfaceTokenUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_token
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_token = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_token);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_token
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_token, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_token, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_token, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_token
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_token
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod0_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod1_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod1_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1::UniffiCallbackInterfaceTokenUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod1_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod0_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod0_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0::UniffiCallbackInterfaceTransactionUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_transaction
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_transaction = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_transaction);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_transaction
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTransactionUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_transaction, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_transaction, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTransactionUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_transaction, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_transaction
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_transaction
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod0_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod1_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod1_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1::UniffiCallbackInterfaceTransactionUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTransactionUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTransactionUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod1_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED

// THIRD: Generate Bridging templates for ALL callbacks FIRST
// Structs will need these
#ifndef BRIDGING_UniffiRustFutureContinuationCallback_DEFINED
#define BRIDGING_UniffiRustFutureContinuationCallback_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiRustFutureContinuationCallbackWrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiRustFutureContinuationCallbackWrapper rsCallbackWrapper) {
    UniffiRustFutureContinuationCallback rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--RustFutureContinuationCallback"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiRustFutureContinuationCallback func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging<int8_t>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiRustFutureContinuationCallback_DEFINED

#ifndef BRIDGING_UniffiForeignFutureDroppedCallback_DEFINED
#define BRIDGING_UniffiForeignFutureDroppedCallback_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureDroppedCallbackWrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureDroppedCallbackWrapper rsCallbackWrapper) {
    UniffiForeignFutureDroppedCallback rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureDroppedCallback"),
        1,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureDroppedCallback func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureDroppedCallback_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiCallbackInterfaceFree_DEFINED
#define BRIDGING_UniffiCallbackInterfaceFree_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceFreeWrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceFreeWrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceFree rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceFree"),
        1,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceFree func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceFree_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceClone_DEFINED
#define BRIDGING_UniffiCallbackInterfaceClone_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceCloneWrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceCloneWrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceClone rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceClone"),
        1,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceClone func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        auto value = func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0])
        );

        
        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceClone_DEFINED

#ifndef BRIDGING_UniffiForeignFutureCompleteU8_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteU8_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteU8Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU8Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteU8 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU8"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU8 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultU8>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteU8_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteI8_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteI8_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteI8Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI8Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteI8 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI8"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI8 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultI8>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteI8_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteU16_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteU16_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteU16Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU16Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteU16 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU16"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU16 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultU16>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteU16_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteI16_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteI16_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteI16Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI16Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteI16 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI16"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI16 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultI16>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteI16_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteU32_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteU32_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteU32Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU32Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteU32 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU32"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU32 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultU32>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteU32_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteI32_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteI32_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteI32Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI32Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteI32 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI32"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI32 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultI32>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteI32_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteU64_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteU64_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteU64Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteU64Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteU64 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteU64"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteU64 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultU64>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteU64_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteI64_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteI64_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteI64Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteI64Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteI64 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteI64"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteI64 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultI64>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteI64_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteF32_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteF32_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteF32Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteF32Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteF32 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteF32"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteF32 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultF32>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteF32_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteF64_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteF64_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteF64Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteF64Wrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteF64 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteF64"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteF64 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultF64>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteF64_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteRustBuffer_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteRustBuffer_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteRustBufferWrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteRustBufferWrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteRustBuffer rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteRustBuffer"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteRustBuffer func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultRustBuffer>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteRustBuffer_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiForeignFutureCompleteVoid_DEFINED
#define BRIDGING_UniffiForeignFutureCompleteVoid_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiForeignFutureCompleteVoidWrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiForeignFutureCompleteVoidWrapper rsCallbackWrapper) {
    UniffiForeignFutureCompleteVoid rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--ForeignFutureCompleteVoid"),
        2,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiForeignFutureCompleteVoid func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<UniffiForeignFutureResultVoid>::fromJs(rt, callInvoker, args[1])
        );

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureCompleteVoid_DEFINED

// CallbackFunction.cpp already generated in second loop
#ifndef BRIDGING_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
#define BRIDGING_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceEntityUpdateCallbackMethod0Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceEntityUpdateCallbackMethod0Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceEntityUpdateCallbackMethod0 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceEntityUpdateCallbackMethod0"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceEntityUpdateCallbackMethod0 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
#define BRIDGING_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceEntityUpdateCallbackMethod1Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceEntityUpdateCallbackMethod1Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceEntityUpdateCallbackMethod1 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceEntityUpdateCallbackMethod1"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceEntityUpdateCallbackMethod1 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
#define BRIDGING_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceEventUpdateCallbackMethod0Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceEventUpdateCallbackMethod0Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceEventUpdateCallbackMethod0 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceEventUpdateCallbackMethod0"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceEventUpdateCallbackMethod0 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
#define BRIDGING_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceEventUpdateCallbackMethod1Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceEventUpdateCallbackMethod1Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceEventUpdateCallbackMethod1 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceEventUpdateCallbackMethod1"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceEventUpdateCallbackMethod1 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
#define BRIDGING_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceTokenBalanceUpdateCallbackMethod0"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
#define BRIDGING_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceTokenBalanceUpdateCallbackMethod1"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
#define BRIDGING_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceTokenUpdateCallbackMethod0Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceTokenUpdateCallbackMethod0Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceTokenUpdateCallbackMethod0 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceTokenUpdateCallbackMethod0"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceTokenUpdateCallbackMethod0 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
#define BRIDGING_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceTokenUpdateCallbackMethod1Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceTokenUpdateCallbackMethod1Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceTokenUpdateCallbackMethod1 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceTokenUpdateCallbackMethod1"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceTokenUpdateCallbackMethod1 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
#define BRIDGING_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceTransactionUpdateCallbackMethod0Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceTransactionUpdateCallbackMethod0Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceTransactionUpdateCallbackMethod0"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED

#ifndef BRIDGING_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
#define BRIDGING_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
namespace uniffi::dojo {
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper struct already declared in CallbackWrapperDecl.cpp

template <> struct Bridging<UniffiCallbackInterfaceTransactionUpdateCallbackMethod1Wrapper> {
  static jsi::Value toJs(jsi::Runtime &rt, std::shared_ptr<CallInvoker> callInvoker, UniffiCallbackInterfaceTransactionUpdateCallbackMethod1Wrapper rsCallbackWrapper) {
    UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 rsCallback = rsCallbackWrapper.callback;
    return jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "--CallbackInterfaceTransactionUpdateCallbackMethod1"),
        3,
        [rsCallback, callInvoker](
            jsi::Runtime &rt,
            const jsi::Value &thisValue,
            const jsi::Value *arguments,
            size_t count) -> jsi::Value
        {
            return intoRust(rt, callInvoker, thisValue, arguments, count, rsCallback);
        }
    );
  }

  static jsi::Value intoRust(
      jsi::Runtime &rt,
      std::shared_ptr<CallInvoker> callInvoker,
      const jsi::Value &thisValue,
      const jsi::Value *args,
      size_t count,
      UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 func) {
    // Convert the arguments into the Rust, with Bridging<T>::fromJs,
    // then call the rs_callback with those arguments.
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        func(uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<void *>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED


// FOURTH: Generate Bridging templates for ALL structs
// These can now use both callback makeCallbackFunction AND callback Bridging templates
#ifndef BRIDGING_UniffiForeignFutureDroppedCallbackStruct_DEFINED
#define BRIDGING_UniffiForeignFutureDroppedCallbackStruct_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureDroppedCallbackStruct> {
  static UniffiForeignFutureDroppedCallbackStruct fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureDroppedCallbackStruct");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureDroppedCallbackStruct rsObject;

    // Create the vtable from the js callbacks.
    rsObject.handle = uniffi_jsi::Bridging<uint64_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "handle")
      );
    rsObject.free = uniffi::dojo::cb::foreignfuturedroppedcallback::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "free")
        );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureDroppedCallbackStruct &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "handle",
      uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rsValue.handle)
    );
    jsObject.setProperty(rt, "free",
      uniffi::dojo::Bridging<UniffiForeignFutureDroppedCallbackWrapper>::toJs(rt, callInvoker, UniffiForeignFutureDroppedCallbackWrapper(rsValue.free))
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureDroppedCallbackStruct_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultU8_DEFINED
#define BRIDGING_UniffiForeignFutureResultU8_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU8> {
  static UniffiForeignFutureResultU8 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU8");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU8 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint8_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU8 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<uint8_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultU8_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultI8_DEFINED
#define BRIDGING_UniffiForeignFutureResultI8_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI8> {
  static UniffiForeignFutureResultI8 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI8");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI8 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int8_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI8 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<int8_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultI8_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultU16_DEFINED
#define BRIDGING_UniffiForeignFutureResultU16_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU16> {
  static UniffiForeignFutureResultU16 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU16");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU16 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint16_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU16 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultU16_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultI16_DEFINED
#define BRIDGING_UniffiForeignFutureResultI16_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI16> {
  static UniffiForeignFutureResultI16 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI16");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI16 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int16_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI16 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<int16_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultI16_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultU32_DEFINED
#define BRIDGING_UniffiForeignFutureResultU32_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU32> {
  static UniffiForeignFutureResultU32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU32");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU32 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint32_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU32 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<uint32_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultU32_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultI32_DEFINED
#define BRIDGING_UniffiForeignFutureResultI32_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI32> {
  static UniffiForeignFutureResultI32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI32");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI32 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int32_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI32 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<int32_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultI32_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultU64_DEFINED
#define BRIDGING_UniffiForeignFutureResultU64_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultU64> {
  static UniffiForeignFutureResultU64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultU64");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultU64 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<uint64_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultU64 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultU64_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultI64_DEFINED
#define BRIDGING_UniffiForeignFutureResultI64_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultI64> {
  static UniffiForeignFutureResultI64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultI64");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultI64 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<int64_t>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultI64 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<int64_t>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultI64_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultF32_DEFINED
#define BRIDGING_UniffiForeignFutureResultF32_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultF32> {
  static UniffiForeignFutureResultF32 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultF32");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultF32 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<float>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultF32 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<float>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultF32_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultF64_DEFINED
#define BRIDGING_UniffiForeignFutureResultF64_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultF64> {
  static UniffiForeignFutureResultF64 fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultF64");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultF64 rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi_jsi::Bridging<double>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultF64 &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi_jsi::Bridging<double>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultF64_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultRustBuffer_DEFINED
#define BRIDGING_UniffiForeignFutureResultRustBuffer_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultRustBuffer> {
  static UniffiForeignFutureResultRustBuffer fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultRustBuffer");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultRustBuffer rsObject;

    // Create the vtable from the js callbacks.
    rsObject.return_value = uniffi::dojo::Bridging<RustBuffer>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "returnValue")
      );
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultRustBuffer &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "returnValue",
      uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rsValue.return_value)
    );
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultRustBuffer_DEFINED
#ifndef BRIDGING_UniffiForeignFutureResultVoid_DEFINED
#define BRIDGING_UniffiForeignFutureResultVoid_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiForeignFutureResultVoid> {
  static UniffiForeignFutureResultVoid fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiForeignFutureResultVoid");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiForeignFutureResultVoid rsObject;

    // Create the vtable from the js callbacks.
    rsObject.call_status = uniffi::dojo::Bridging<RustCallStatus>::fromJs(
        rt, callInvoker,
        jsObject.getProperty(rt, "callStatus")
      );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiForeignFutureResultVoid &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "callStatus",
      uniffi::dojo::Bridging<RustCallStatus>::toJs(rt, callInvoker, rsValue.call_status)
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiForeignFutureResultVoid_DEFINED
#ifndef BRIDGING_UniffiVTableCallbackInterfaceEntityUpdateCallback_DEFINED
#define BRIDGING_UniffiVTableCallbackInterfaceEntityUpdateCallback_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceEntityUpdateCallback> {
  static UniffiVTableCallbackInterfaceEntityUpdateCallback fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceEntityUpdateCallback");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceEntityUpdateCallback rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::dojo::cb::callbackinterfaceclone::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.on_update = uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onUpdate")
        );
    rsObject.on_error = uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onError")
        );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiVTableCallbackInterfaceEntityUpdateCallback &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "uniffiFree",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceFreeWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceFreeWrapper(rsValue.uniffi_free))
    );
    jsObject.setProperty(rt, "uniffiClone",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceCloneWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceCloneWrapper(rsValue.uniffi_clone))
    );
    jsObject.setProperty(rt, "onUpdate",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceEntityUpdateCallbackMethod0Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceEntityUpdateCallbackMethod0Wrapper(rsValue.on_update))
    );
    jsObject.setProperty(rt, "onError",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceEntityUpdateCallbackMethod1Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceEntityUpdateCallbackMethod1Wrapper(rsValue.on_error))
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiVTableCallbackInterfaceEntityUpdateCallback_DEFINED
#ifndef BRIDGING_UniffiVTableCallbackInterfaceEventUpdateCallback_DEFINED
#define BRIDGING_UniffiVTableCallbackInterfaceEventUpdateCallback_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceEventUpdateCallback> {
  static UniffiVTableCallbackInterfaceEventUpdateCallback fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceEventUpdateCallback");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceEventUpdateCallback rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::dojo::cb::callbackinterfaceclone::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.on_update = uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onUpdate")
        );
    rsObject.on_error = uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onError")
        );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiVTableCallbackInterfaceEventUpdateCallback &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "uniffiFree",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceFreeWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceFreeWrapper(rsValue.uniffi_free))
    );
    jsObject.setProperty(rt, "uniffiClone",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceCloneWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceCloneWrapper(rsValue.uniffi_clone))
    );
    jsObject.setProperty(rt, "onUpdate",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceEventUpdateCallbackMethod0Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceEventUpdateCallbackMethod0Wrapper(rsValue.on_update))
    );
    jsObject.setProperty(rt, "onError",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceEventUpdateCallbackMethod1Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceEventUpdateCallbackMethod1Wrapper(rsValue.on_error))
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiVTableCallbackInterfaceEventUpdateCallback_DEFINED
#ifndef BRIDGING_UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback_DEFINED
#define BRIDGING_UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback> {
  static UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::dojo::cb::callbackinterfaceclone::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.on_update = uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onUpdate")
        );
    rsObject.on_error = uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onError")
        );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "uniffiFree",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceFreeWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceFreeWrapper(rsValue.uniffi_free))
    );
    jsObject.setProperty(rt, "uniffiClone",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceCloneWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceCloneWrapper(rsValue.uniffi_clone))
    );
    jsObject.setProperty(rt, "onUpdate",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0Wrapper(rsValue.on_update))
    );
    jsObject.setProperty(rt, "onError",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1Wrapper(rsValue.on_error))
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback_DEFINED
#ifndef BRIDGING_UniffiVTableCallbackInterfaceTokenUpdateCallback_DEFINED
#define BRIDGING_UniffiVTableCallbackInterfaceTokenUpdateCallback_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceTokenUpdateCallback> {
  static UniffiVTableCallbackInterfaceTokenUpdateCallback fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceTokenUpdateCallback");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceTokenUpdateCallback rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::dojo::cb::callbackinterfaceclone::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.on_update = uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onUpdate")
        );
    rsObject.on_error = uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onError")
        );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiVTableCallbackInterfaceTokenUpdateCallback &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "uniffiFree",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceFreeWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceFreeWrapper(rsValue.uniffi_free))
    );
    jsObject.setProperty(rt, "uniffiClone",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceCloneWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceCloneWrapper(rsValue.uniffi_clone))
    );
    jsObject.setProperty(rt, "onUpdate",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceTokenUpdateCallbackMethod0Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceTokenUpdateCallbackMethod0Wrapper(rsValue.on_update))
    );
    jsObject.setProperty(rt, "onError",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceTokenUpdateCallbackMethod1Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceTokenUpdateCallbackMethod1Wrapper(rsValue.on_error))
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiVTableCallbackInterfaceTokenUpdateCallback_DEFINED
#ifndef BRIDGING_UniffiVTableCallbackInterfaceTransactionUpdateCallback_DEFINED
#define BRIDGING_UniffiVTableCallbackInterfaceTransactionUpdateCallback_DEFINED
namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

template <> struct Bridging<UniffiVTableCallbackInterfaceTransactionUpdateCallback> {
  static UniffiVTableCallbackInterfaceTransactionUpdateCallback fromJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &jsValue
  ) {
    // Check if the input is an object
    if (!jsValue.isObject()) {
      throw jsi::JSError(rt, "Expected an object for UniffiVTableCallbackInterfaceTransactionUpdateCallback");
    }

    // Get the object from the jsi::Value
    auto jsObject = jsValue.getObject(rt);

    // Create the vtable struct
    UniffiVTableCallbackInterfaceTransactionUpdateCallback rsObject;

    // Create the vtable from the js callbacks.
    rsObject.uniffi_free = uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiFree")
        );
    rsObject.uniffi_clone = uniffi::dojo::cb::callbackinterfaceclone::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "uniffiClone")
        );
    rsObject.on_update = uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onUpdate")
        );
    rsObject.on_error = uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1::makeCallbackFunction(
          rt, callInvoker, jsObject.getProperty(rt, "onError")
        );

    return rsObject;
  }

  static jsi::Value toJs(jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const UniffiVTableCallbackInterfaceTransactionUpdateCallback &rsValue
  ) {
    // Create a JS object
    auto jsObject = jsi::Object(rt);

    // Convert each field from Rust to JS
    jsObject.setProperty(rt, "uniffiFree",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceFreeWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceFreeWrapper(rsValue.uniffi_free))
    );
    jsObject.setProperty(rt, "uniffiClone",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceCloneWrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceCloneWrapper(rsValue.uniffi_clone))
    );
    jsObject.setProperty(rt, "onUpdate",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceTransactionUpdateCallbackMethod0Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceTransactionUpdateCallbackMethod0Wrapper(rsValue.on_update))
    );
    jsObject.setProperty(rt, "onError",
      uniffi::dojo::Bridging<UniffiCallbackInterfaceTransactionUpdateCallbackMethod1Wrapper>::toJs(rt, callInvoker, UniffiCallbackInterfaceTransactionUpdateCallbackMethod1Wrapper(rsValue.on_error))
    );

    return jsObject;
  }
};

} // namespace uniffi::dojo
#endif // BRIDGING_UniffiVTableCallbackInterfaceTransactionUpdateCallback_DEFINED
    // Implementation of callback function calling from Rust to JS RustFutureContinuationCallback
#ifndef CALLBACK_uniffi_dojo_cb_rustfuturecontinuationcallback_UniffiRustFutureContinuationCallback_DEFINED
#define CALLBACK_uniffi_dojo_cb_rustfuturecontinuationcallback_UniffiRustFutureContinuationCallback_DEFINED
// Callback function: uniffi::dojo::cb::rustfuturecontinuationcallback::UniffiRustFutureContinuationCallback
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::rustfuturecontinuationcallback {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, int8_t)> rsLambda_UniffiRustFutureContinuationCallback = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiRustFutureContinuationCallback(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_data
            ,int8_t rs_pollResult) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_data = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_data);
        auto js_pollResult = uniffi_jsi::Bridging<int8_t>::toJs(rt, callInvoker, rs_pollResult);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_data, js_pollResult
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiRustFutureContinuationCallback: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiRustFutureContinuationCallback(uint64_t rs_data, int8_t rs_pollResult) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiRustFutureContinuationCallback.
        //
        // If rsLamda_UniffiRustFutureContinuationCallback is null, then there is no runtime to call into.
        if (rsLambda_UniffiRustFutureContinuationCallback == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiRustFutureContinuationCallback(
            rs_data, 
            rs_pollResult);
    }

    [[maybe_unused]] static UniffiRustFutureContinuationCallback
    makeCallbackFunction( // uniffi::dojo::cb::rustfuturecontinuationcallback
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiRustFutureContinuationCallback != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiRustFutureContinuationCallback;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiRustFutureContinuationCallback = [rtPtr, callInvoker, callbackValue](uint64_t rs_data, int8_t rs_pollResult) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_data
                    , rs_pollResult](jsi::Runtime &rt) mutable {
                    body_UniffiRustFutureContinuationCallback(rt, callInvoker, callbackValue
                        , rs_data
                        , rs_pollResult);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiRustFutureContinuationCallback;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiRustFutureContinuationCallback = nullptr;
    }
} // namespace uniffi::dojo::cb::rustfuturecontinuationcallback
#endif // CALLBACK_uniffi_dojo_cb_rustfuturecontinuationcallback_UniffiRustFutureContinuationCallback_DEFINED
    // JS-to-Rust callbacks already generated in first loop
    // Implementation of free callback function CallbackInterfaceFree

#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceentityupdatecallback_vtablecallbackinterfaceentityupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceentityupdatecallback_vtablecallbackinterfaceentityupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceentityupdatecallback_vtablecallbackinterfaceentityupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceeventupdatecallback_vtablecallbackinterfaceeventupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceeventupdatecallback_vtablecallbackinterfaceeventupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfaceeventupdatecallback_vtablecallbackinterfaceeventupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenbalanceupdatecallback_vtablecallbackinterfacetokenbalanceupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenbalanceupdatecallback_vtablecallbackinterfacetokenbalanceupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenbalanceupdatecallback_vtablecallbackinterfacetokenbalanceupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenupdatecallback_vtablecallbackinterfacetokenupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenupdatecallback_vtablecallbackinterfacetokenupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetokenupdatecallback_vtablecallbackinterfacetokenupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#ifndef CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetransactionupdatecallback_vtablecallbackinterfacetransactionupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
#define CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetransactionupdatecallback_vtablecallbackinterfacetransactionupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
// Callback function: uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free::UniffiCallbackInterfaceFree
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t)> rsLambda_UniffiCallbackInterfaceFree = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceFree(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceFree: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceFree(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceFree.
        //
        // If rsLamda_UniffiCallbackInterfaceFree is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceFree == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceFree(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceFree
    makeCallbackFunction( // uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceFree != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceFree;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceFree = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceFree(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceFree;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceFree = nullptr;
    }
} // namespace uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free
#endif // CALLBACK_uniffi_dojo_st_vtablecallbackinterfacetransactionupdatecallback_vtablecallbackinterfacetransactionupdatecallback_free_UniffiCallbackInterfaceFree_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceClone
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceclone_UniffiCallbackInterfaceClone_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceclone_UniffiCallbackInterfaceClone_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceclone::UniffiCallbackInterfaceClone
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceclone {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<uint64_t(uint64_t)> rsLambda_UniffiCallbackInterfaceClone = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceClone(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_handle) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_handle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_handle);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_handle
            );

            

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceClone: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static uint64_t callback_UniffiCallbackInterfaceClone(uint64_t rs_handle) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceClone.
        //
        // If rsLamda_UniffiCallbackInterfaceClone is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceClone == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return 0;  // Return zero for handle/uint64_t return types
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        return rsLambda_UniffiCallbackInterfaceClone(
            rs_handle);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceClone
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceclone
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceClone != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceClone;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceClone = [rtPtr, callInvoker, callbackValue](uint64_t rs_handle) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_handle](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceClone(rt, callInvoker, callbackValue
                        , rs_handle);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                
                callInvoker->invokeNonBlocking(*rtPtr, jsLambda);
                return 0;  // Async callback, return immediately
        };
        return callback_UniffiCallbackInterfaceClone;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceClone = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceclone
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceclone_UniffiCallbackInterfaceClone_DEFINED
    // Structs already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Structs already generated in first loop
    // JS-to-Rust callbacks already generated in first loop
    // Implementation of callback function calling from Rust to JS CallbackInterfaceEntityUpdateCallbackMethod0
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod0_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod0_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0::UniffiCallbackInterfaceEntityUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_entity
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_entity = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_entity);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_entity
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEntityUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_entity, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_entity, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEntityUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_entity, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_entity
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEntityUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_entity
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod0_UniffiCallbackInterfaceEntityUpdateCallbackMethod0_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceEntityUpdateCallbackMethod1
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod1_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod1_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1::UniffiCallbackInterfaceEntityUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEntityUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEntityUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEntityUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEntityUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEntityUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceentityupdatecallbackmethod1_UniffiCallbackInterfaceEntityUpdateCallbackMethod1_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceEventUpdateCallbackMethod0
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod0_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod0_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0::UniffiCallbackInterfaceEventUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEventUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_event
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_event = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_event);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_event
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEventUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEventUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_event, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_event, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEventUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_event, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_event
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEventUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_event
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod0_UniffiCallbackInterfaceEventUpdateCallbackMethod0_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceEventUpdateCallbackMethod1
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod1_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod1_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1::UniffiCallbackInterfaceEventUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceEventUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceEventUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceEventUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceEventUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceEventUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceEventUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceEventUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfaceeventupdatecallbackmethod1_UniffiCallbackInterfaceEventUpdateCallbackMethod1_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceTokenBalanceUpdateCallbackMethod0
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod0_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod0_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0::UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_balance
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_balance = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_balance);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_balance
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_balance, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_balance, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_balance, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_balance
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_balance
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod0_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod0_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceTokenBalanceUpdateCallbackMethod1
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod1_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod1_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1::UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenbalanceupdatecallbackmethod1_UniffiCallbackInterfaceTokenBalanceUpdateCallbackMethod1_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceTokenUpdateCallbackMethod0
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod0_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod0_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0::UniffiCallbackInterfaceTokenUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_token
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_token = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_token);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_token
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_token, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_token, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_token, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_token
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_token
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod0_UniffiCallbackInterfaceTokenUpdateCallbackMethod0_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceTokenUpdateCallbackMethod1
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod1_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod1_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1::UniffiCallbackInterfaceTokenUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTokenUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTokenUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTokenUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTokenUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTokenUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetokenupdatecallbackmethod1_UniffiCallbackInterfaceTokenUpdateCallbackMethod1_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceTransactionUpdateCallbackMethod0
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod0_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod0_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0::UniffiCallbackInterfaceTransactionUpdateCallbackMethod0
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_transaction
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_transaction = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_transaction);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_transaction
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTransactionUpdateCallbackMethod0: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(uint64_t rs_uniffiHandle, RustBuffer rs_transaction, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0.
        //
        // If rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(
            rs_uniffiHandle, 
            rs_transaction, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTransactionUpdateCallbackMethod0
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_transaction, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_transaction
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_transaction
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod0_UniffiCallbackInterfaceTransactionUpdateCallbackMethod0_DEFINED
    // Implementation of callback function calling from Rust to JS CallbackInterfaceTransactionUpdateCallbackMethod1
#ifndef CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod1_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
#define CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod1_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
// Callback function: uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1::UniffiCallbackInterfaceTransactionUpdateCallbackMethod1
//
// We have the following constraints:
// - we need to pass a function pointer to Rust.
// - we need a jsi::Runtime and jsi::Function to call into JS.
// - function pointers can't store state, so we can't use a lamda.
//
// For this, we store a lambda as a global, as `rsLambda`. The `callback` function calls
// the lambda, which itself calls the `body` which then calls into JS.
//
// We then give the `callback` function pointer to Rust which will call the lambda sometime in the
// future.
namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1 {
    using namespace facebook;

    // We need to store a lambda in a global so we can call it from
    // a function pointer. The function pointer is passed to Rust.
    static std::function<void(uint64_t, RustBuffer, void *, RustCallStatus*)> rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 = nullptr;

    // This is the main body of the callback. It's called from the lambda,
    // which itself is called from the callback function which is passed to Rust.
    static void body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     std::shared_ptr<jsi::Value> callbackValue
            ,uint64_t rs_uniffiHandle
            ,RustBuffer rs_error
            ,void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {

        // Convert the arguments from Rust, into jsi::Values.
        // We'll use the Bridging class to do this…
        auto js_uniffiHandle = uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, rs_uniffiHandle);
        auto js_error = uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, rs_error);

        // Now we are ready to call the callback.
        // We are already on the JS thread, because this `body` function was
        // invoked from the CallInvoker.
        try {
            // Getting the callback function
            auto cb = callbackValue->asObject(rt).asFunction(rt);
            auto uniffiResult = cb.call(rt, js_uniffiHandle, js_error
            );

            // Now copy the result back from JS into the RustCallStatus object.
            uniffi::dojo::Bridging<RustCallStatus>::copyFromJs(rt, callInvoker, uniffiResult, uniffi_call_status);

            if (uniffi_call_status->code != UNIFFI_CALL_STATUS_OK) {
                // The JS callback finished abnormally, so we cannot retrieve the return value.
                return;
            }

            
        } catch (const jsi::JSError &error) {
            std::cout << "Error in callback UniffiCallbackInterfaceTransactionUpdateCallbackMethod1: "
                    << error.what() << std::endl;
            throw error;
        }
    }

    static void callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
        // If the runtime has shutdown, then there is no point in trying to
        // call into Javascript. BUT how do we tell if the runtime has shutdown?
        //
        // Answer: the module destructor calls into callback `cleanup` method,
        // which nulls out the rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1.
        //
        // If rsLamda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 is null, then there is no runtime to call into.
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 == nullptr) {
            // This only occurs when destructors are calling into Rust free/drop,
            // which causes the JS callback to be dropped.
            return;
        }

        // The runtime, the actual callback jsi::funtion, and the callInvoker
        // are all in the lambda.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(
            rs_uniffiHandle, 
            rs_error, 
            rs_uniffiOutReturn, uniffi_call_status);
    }

    [[maybe_unused]] static UniffiCallbackInterfaceTransactionUpdateCallbackMethod1
    makeCallbackFunction( // uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1
                    jsi::Runtime &rt,
                     std::shared_ptr<uniffi_runtime::UniffiCallInvoker> callInvoker,
                     const jsi::Value &value) {
        if (rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 != nullptr) {
            // `makeCallbackFunction` is called in two circumstances:
            //
            // 1. at startup, when initializing callback interface vtables.
            // 2. when polling futures. This happens at least once per future that is
            //    exposed to Javascript. We know that this is always the same function,
            //    `uniffiFutureContinuationCallback` in `async-rust-calls.ts`.
            //
            // We can therefore return the callback function without making anything
            // new if we've been initialized already.
            return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1;
        }
        auto callbackFunction = value.asObject(rt).asFunction(rt);
        auto callbackValue = std::make_shared<jsi::Value>(rt, callbackFunction);
        // Store a raw pointer to the runtime. This is safe because:
        // 1. The runtime is owned by React Native and persists for the app lifetime
        // 2. The cleanup() method is called when the runtime is destroyed, which nulls out rsLambda
        jsi::Runtime *rtPtr = &rt;
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 = [rtPtr, callInvoker, callbackValue](uint64_t rs_uniffiHandle, RustBuffer rs_error, void * rs_uniffiOutReturn, RustCallStatus* uniffi_call_status) {
                // We immediately make a lambda which will do the work of transforming the
                // arguments into JSI values and calling the callback.
                uniffi_runtime::UniffiCallFunc jsLambda = [
                    callInvoker,
                    callbackValue
                    , rs_uniffiHandle
                    , rs_error
                    , rs_uniffiOutReturn, uniffi_call_status](jsi::Runtime &rt) mutable {
                    body_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1(rt, callInvoker, callbackValue
                        , rs_uniffiHandle
                        , rs_error
                        , rs_uniffiOutReturn, uniffi_call_status);
                };
                // We'll then call that lambda from the callInvoker which will
                // look after calling it on the correct thread.
                callInvoker->invokeBlocking(*rtPtr, jsLambda);
        };
        return callback_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1;
    }

    // This method is called from the destructor of NativeDojo, which only happens
    // when the jsi::Runtime is being destroyed.
    [[maybe_unused]] static void cleanup() {
        // The lambda holds a reference to the the Runtime, so when this is nulled out,
        // then the pointer will no longer be left dangling.
        rsLambda_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1 = nullptr;
    }
} // namespace uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1
#endif // CALLBACK_uniffi_dojo_cb_callbackinterfacetransactionupdatecallbackmethod1_UniffiCallbackInterfaceTransactionUpdateCallbackMethod1_DEFINED
    // Structs already generated in first loop
    // Structs already generated in first loop
    // Structs already generated in first loop
    // Structs already generated in first loop
    // Structs already generated in first loop


namespace uniffi::dojo {
using namespace facebook;
using CallInvoker = uniffi_runtime::UniffiCallInvoker;

// Wrapper to make this callback unique per module  
struct UniffiRustFutureContinuationCallbackDojoWrapper {
    UniffiRustFutureContinuationCallback callback;
    explicit UniffiRustFutureContinuationCallbackDojoWrapper(UniffiRustFutureContinuationCallback cb) : callback(cb) {}
    operator UniffiRustFutureContinuationCallback() const { return callback; }
};

template <> struct Bridging<UniffiRustFutureContinuationCallbackDojoWrapper> {
  static UniffiRustFutureContinuationCallbackDojoWrapper fromJs(
    jsi::Runtime &rt,
    std::shared_ptr<CallInvoker> callInvoker,
    const jsi::Value &value
  ) {
    try {
      return UniffiRustFutureContinuationCallbackDojoWrapper(
        uniffi::dojo::cb::rustfuturecontinuationcallback::makeCallbackFunction(
          rt,
          callInvoker,
          value
        )
      );
    } catch (const std::logic_error &e) {
      throw jsi::JSError(rt, e.what());
    }
  }
};

} // namespace uniffi::dojo

NativeDojo::NativeDojo(
    jsi::Runtime &rt,
    std::shared_ptr<uniffi_runtime::UniffiCallInvoker> invoker
) : callInvoker(invoker), props() {
    // Map from Javascript names to the cpp names
    props["ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_func_ffi__string_to_byte_length(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_func_ffi__string_to_arraybuffer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_func_ffi__string_to_arraybuffer"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_func_ffi__string_to_arraybuffer(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_internal_fn_func_ffi__arraybuffer_to_string"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_internal_fn_func_ffi__arraybuffer_to_string"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_internal_fn_func_ffi__arraybuffer_to_string(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_clone_toriiclient"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_clone_toriiclient"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_clone_toriiclient(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_free_toriiclient"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_free_toriiclient"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_free_toriiclient(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_constructor_toriiclient_new"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_constructor_toriiclient_new"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_constructor_toriiclient_new(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_constructor_toriiclient_new_with_config"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_constructor_toriiclient_new_with_config"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_constructor_toriiclient_new_with_config(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_achievements"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_achievements"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_achievements(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_activities"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_activities"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_activities(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_aggregations"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_aggregations"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_aggregations(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_cancel_subscription"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_cancel_subscription"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_cancel_subscription(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_contracts"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_contracts"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_contracts(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_controllers"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_controllers"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_controllers(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_entities"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_entities"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_entities(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_event_messages"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_event_messages"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_event_messages(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_player_achievements"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_player_achievements"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_player_achievements(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message_batch"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message_batch"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message_batch(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_search"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_search"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_search(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_sql"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_sql"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_sql(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_starknet_events"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_starknet_events"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_starknet_events(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_entity_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_entity_updates"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_entity_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_event_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_event_updates"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_event_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_balance_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_balance_updates"),
        5,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_balance_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_updates"),
        4,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_transaction_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_transaction_updates"),
        3,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_transaction_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_token_balances"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_token_balances"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_token_balances(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_token_contracts"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_token_contracts"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_token_contracts(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_token_transfers"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_token_transfers"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_token_transfers(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_tokens"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_tokens"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_tokens(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_transactions"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_transactions"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_transactions(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_worlds"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_method_toriiclient_worlds"),
        2,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_method_toriiclient_worlds(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_entityupdatecallback"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_entityupdatecallback"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_entityupdatecallback(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_eventupdatecallback"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_eventupdatecallback"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_eventupdatecallback(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenbalanceupdatecallback"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenbalanceupdatecallback"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenbalanceupdatecallback(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenupdatecallback"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenupdatecallback"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenupdatecallback(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_transactionupdatecallback"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_fn_init_callback_vtable_transactionupdatecallback"),
        1,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_transactionupdatecallback(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_achievements"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_achievements"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_achievements(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_activities"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_activities"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_activities(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_aggregations"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_aggregations"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_aggregations(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_cancel_subscription"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_cancel_subscription"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_cancel_subscription(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_contracts"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_contracts"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_contracts(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_controllers"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_controllers"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_controllers(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_entities"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_entities"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_entities(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_event_messages"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_event_messages"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_event_messages(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_player_achievements"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_player_achievements"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_player_achievements(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message_batch"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message_batch"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message_batch(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_search"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_search"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_search(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_sql"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_sql"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_sql(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_starknet_events"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_starknet_events"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_starknet_events(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_entity_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_entity_updates"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_entity_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_event_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_event_updates"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_event_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_balance_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_balance_updates"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_balance_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_updates"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_transaction_updates"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_transaction_updates"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_transaction_updates(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_token_balances"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_token_balances"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_token_balances(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_token_contracts"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_token_contracts"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_token_contracts(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_token_transfers"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_token_transfers"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_token_transfers(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_tokens"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_tokens"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_tokens(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_transactions"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_transactions"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_transactions(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_worlds"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_toriiclient_worlds"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_worlds(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new_with_config"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new_with_config"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new_with_config(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_update"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_update"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_update(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_error"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_error"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_error(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_update"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_update"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_update(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_error"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_error"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_error(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_update"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_update"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_update(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_error"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_error"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_error(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_update"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_update"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_update(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_error"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_error"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_error(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_update"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_update"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_update(rt, thisVal, args, count);
        }
    );
    props["ubrn_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_error"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_error"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_error(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_dojo_uniffi_uniffi_contract_version"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_dojo_uniffi_uniffi_contract_version"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_dojo_uniffi_uniffi_contract_version(rt, thisVal, args, count);
        }
    );
    props["ubrn_ffi_ToriiClient__bless_pointer"] = jsi::Function::createFromHostFunction(
        rt,
        jsi::PropNameID::forAscii(rt, "ubrn_ffi_ToriiClient__bless_pointer"),
        0,
        [this](jsi::Runtime &rt, const jsi::Value &thisVal, const jsi::Value *args, size_t count) -> jsi::Value {
            return this->cpp_ffi_ToriiClient__bless_pointer(rt, thisVal, args, count);
        }
    );
}

void NativeDojo::registerModule(jsi::Runtime &rt, std::shared_ptr<react::CallInvoker> callInvoker) {
    auto invoker = std::make_shared<uniffi_runtime::UniffiCallInvoker>(callInvoker);
    auto tm = std::make_shared<NativeDojo>(rt, invoker);
    auto obj = rt.global().createFromHostObject(rt, tm);
    rt.global().setProperty(rt, "NativeDojo", obj);
}

void NativeDojo::unregisterModule(jsi::Runtime &rt) {
    uniffi::dojo::registry::clearRegistry();
}

jsi::Value NativeDojo::get(jsi::Runtime& rt, const jsi::PropNameID& name) {
    try {
        return jsi::Value(rt, props.at(name.utf8(rt)));
    }
    catch (std::out_of_range &e) {
        return jsi::Value::undefined();
    }
}

std::vector<jsi::PropNameID> NativeDojo::getPropertyNames(jsi::Runtime& rt) {
    std::vector<jsi::PropNameID> rval;
    for (auto& [key, value] : props) {
        rval.push_back(jsi::PropNameID::forUtf8(rt, key));
    }
    return rval;
}

void NativeDojo::set(jsi::Runtime& rt, const jsi::PropNameID& name, const jsi::Value& value) {
    props.insert_or_assign(name.utf8(rt), &value);
}

NativeDojo::~NativeDojo() {
    // Cleanup for callback function RustFutureContinuationCallback
uniffi::dojo::cb::rustfuturecontinuationcallback::cleanup();
    // Cleanup for "free" callback function CallbackInterfaceFree
uniffi::dojo::st::vtablecallbackinterfaceentityupdatecallback::vtablecallbackinterfaceentityupdatecallback::free::cleanup();uniffi::dojo::st::vtablecallbackinterfaceeventupdatecallback::vtablecallbackinterfaceeventupdatecallback::free::cleanup();uniffi::dojo::st::vtablecallbackinterfacetokenbalanceupdatecallback::vtablecallbackinterfacetokenbalanceupdatecallback::free::cleanup();uniffi::dojo::st::vtablecallbackinterfacetokenupdatecallback::vtablecallbackinterfacetokenupdatecallback::free::cleanup();uniffi::dojo::st::vtablecallbackinterfacetransactionupdatecallback::vtablecallbackinterfacetransactionupdatecallback::free::cleanup();
    // Cleanup for callback function CallbackInterfaceClone
uniffi::dojo::cb::callbackinterfaceclone::cleanup();
    // Cleanup for callback function CallbackInterfaceEntityUpdateCallbackMethod0
uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod0::cleanup();
    // Cleanup for callback function CallbackInterfaceEntityUpdateCallbackMethod1
uniffi::dojo::cb::callbackinterfaceentityupdatecallbackmethod1::cleanup();
    // Cleanup for callback function CallbackInterfaceEventUpdateCallbackMethod0
uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod0::cleanup();
    // Cleanup for callback function CallbackInterfaceEventUpdateCallbackMethod1
uniffi::dojo::cb::callbackinterfaceeventupdatecallbackmethod1::cleanup();
    // Cleanup for callback function CallbackInterfaceTokenBalanceUpdateCallbackMethod0
uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod0::cleanup();
    // Cleanup for callback function CallbackInterfaceTokenBalanceUpdateCallbackMethod1
uniffi::dojo::cb::callbackinterfacetokenbalanceupdatecallbackmethod1::cleanup();
    // Cleanup for callback function CallbackInterfaceTokenUpdateCallbackMethod0
uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod0::cleanup();
    // Cleanup for callback function CallbackInterfaceTokenUpdateCallbackMethod1
uniffi::dojo::cb::callbackinterfacetokenupdatecallbackmethod1::cleanup();
    // Cleanup for callback function CallbackInterfaceTransactionUpdateCallbackMethod0
uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod0::cleanup();
    // Cleanup for callback function CallbackInterfaceTransactionUpdateCallbackMethod1
uniffi::dojo::cb::callbackinterfacetransactionupdatecallbackmethod1::cleanup();
}

// Utility functions for serialization/deserialization of strings.
jsi::Value NativeDojo::cpp_uniffi_internal_fn_func_ffi__string_to_byte_length(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    return uniffi_jsi::Bridging<std::string>::string_to_bytelength(rt, args[0]);
}

jsi::Value NativeDojo::cpp_uniffi_internal_fn_func_ffi__string_to_arraybuffer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    return uniffi_jsi::Bridging<std::string>::string_to_arraybuffer(rt, args[0]);
}

jsi::Value NativeDojo::cpp_uniffi_internal_fn_func_ffi__arraybuffer_to_string(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    return uniffi_jsi::Bridging<std::string>::arraybuffer_to_string(rt, args[0]);
}jsi::Value NativeDojo::cpp_ffi_ToriiClient__bless_pointer(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto pointer = uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[0]);
    auto static destructor = [](uint64_t p) {
        RustCallStatus status = {0};
        uniffi_dojo_uniffi_fn_free_toriiclient(p, &status);
    };
    auto ptrObj = std::make_shared<uniffi_jsi::DestructibleObject>(pointer, destructor);
    auto obj = jsi::Object::createFromHostObject(rt, ptrObj);
    return jsi::Value(rt, obj);
}

// Methods calling directly into the uniffi generated C API of the Rust crate.
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_clone_toriiclient(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_clone_toriiclient(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_free_toriiclient(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_dojo_uniffi_fn_free_toriiclient(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_constructor_toriiclient_new(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_constructor_toriiclient_new(uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[0]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_constructor_toriiclient_new_with_config(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_constructor_toriiclient_new_with_config(uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging</*handle*/ uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_achievements(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_achievements(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_activities(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_activities(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_aggregations(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_aggregations(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_cancel_subscription(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        uniffi_dojo_uniffi_fn_method_toriiclient_cancel_subscription(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_contracts(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_contracts(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_controllers(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_controllers(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_entities(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_entities(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_event_messages(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_event_messages(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_player_achievements(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_player_achievements(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_publish_message(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_publish_message_batch(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_publish_message_batch(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_search(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_search(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_sql(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_sql(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_starknet_events(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_starknet_events(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_entity_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_entity_updates(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[3]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_event_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_event_updates(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_balance_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_balance_updates(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[3]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[4]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_token_updates(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[2]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[3]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_transaction_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_subscribe_transaction_updates(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), uniffi_jsi::Bridging<uint64_t>::fromJs(rt, callInvoker, args[2]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi_jsi::Bridging<uint64_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_token_balances(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_token_balances(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_token_contracts(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_token_contracts(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_token_transfers(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_token_transfers(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_tokens(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_tokens(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_transactions(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_transactions(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_method_toriiclient_worlds(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        RustCallStatus status = uniffi::dojo::Bridging<RustCallStatus>::rustSuccess(rt);
        auto value = uniffi_dojo_uniffi_fn_method_toriiclient_worlds(uniffi_jsi::Bridging</*handle*/ uint64_t>::fromJs(rt, callInvoker, args[0]), uniffi::dojo::Bridging<RustBuffer>::fromJs(rt, callInvoker, args[1]), 
            &status
        );
        uniffi::dojo::Bridging<RustCallStatus>::copyIntoJs(rt, callInvoker, status, args[count - 1]);

        
        return uniffi::dojo::Bridging<RustBuffer>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_entityupdatecallback(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::dojo::Bridging<UniffiVTableCallbackInterfaceEntityUpdateCallback>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::dojo::registry::vtableMutex);
    uniffi_dojo_uniffi_fn_init_callback_vtable_entityupdatecallback(
        uniffi::dojo::registry::putTable(
            "UniffiVTableCallbackInterfaceEntityUpdateCallback",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_eventupdatecallback(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::dojo::Bridging<UniffiVTableCallbackInterfaceEventUpdateCallback>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::dojo::registry::vtableMutex);
    uniffi_dojo_uniffi_fn_init_callback_vtable_eventupdatecallback(
        uniffi::dojo::registry::putTable(
            "UniffiVTableCallbackInterfaceEventUpdateCallback",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenbalanceupdatecallback(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::dojo::Bridging<UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::dojo::registry::vtableMutex);
    uniffi_dojo_uniffi_fn_init_callback_vtable_tokenbalanceupdatecallback(
        uniffi::dojo::registry::putTable(
            "UniffiVTableCallbackInterfaceTokenBalanceUpdateCallback",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_tokenupdatecallback(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::dojo::Bridging<UniffiVTableCallbackInterfaceTokenUpdateCallback>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::dojo::registry::vtableMutex);
    uniffi_dojo_uniffi_fn_init_callback_vtable_tokenupdatecallback(
        uniffi::dojo::registry::putTable(
            "UniffiVTableCallbackInterfaceTokenUpdateCallback",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_fn_init_callback_vtable_transactionupdatecallback(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
    auto vtableInstance =
        uniffi::dojo::Bridging<UniffiVTableCallbackInterfaceTransactionUpdateCallback>::fromJs(
            rt,
            callInvoker,
            args[0]
        );

    std::lock_guard<std::mutex> lock(uniffi::dojo::registry::vtableMutex);
    uniffi_dojo_uniffi_fn_init_callback_vtable_transactionupdatecallback(
        uniffi::dojo::registry::putTable(
            "UniffiVTableCallbackInterfaceTransactionUpdateCallback",
            vtableInstance
        )
    );
    return jsi::Value::undefined();
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_achievements(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_achievements(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_activities(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_activities(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_aggregations(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_aggregations(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_cancel_subscription(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_cancel_subscription(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_contracts(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_contracts(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_controllers(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_controllers(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_entities(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_entities(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_event_messages(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_event_messages(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_player_achievements(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_player_achievements(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message_batch(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_publish_message_batch(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_search(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_search(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_sql(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_sql(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_starknet_events(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_starknet_events(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_entity_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_entity_updates(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_event_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_event_updates(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_balance_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_balance_updates(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_token_updates(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_transaction_updates(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_subscribe_transaction_updates(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_token_balances(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_token_balances(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_token_contracts(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_token_contracts(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_token_transfers(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_token_transfers(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_tokens(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_tokens(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_transactions(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_transactions(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_toriiclient_worlds(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_toriiclient_worlds(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_constructor_toriiclient_new(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_constructor_toriiclient_new_with_config(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_constructor_toriiclient_new_with_config(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_update(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_update(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_error(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_entityupdatecallback_on_error(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_update(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_update(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_error(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_eventupdatecallback_on_error(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_update(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_update(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_error(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_tokenbalanceupdatecallback_on_error(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_update(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_update(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_error(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_tokenupdatecallback_on_error(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_update(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_update(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_error(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = uniffi_dojo_uniffi_checksum_method_transactionupdatecallback_on_error(
        );

        
        return uniffi_jsi::Bridging<uint16_t>::toJs(rt, callInvoker, value);
}
jsi::Value NativeDojo::cpp_ffi_dojo_uniffi_uniffi_contract_version(jsi::Runtime& rt, const jsi::Value& thisVal, const jsi::Value* args, size_t count) {
        auto value = ffi_dojo_uniffi_uniffi_contract_version(
        );

        
        return uniffi_jsi::Bridging<uint32_t>::toJs(rt, callInvoker, value);
}